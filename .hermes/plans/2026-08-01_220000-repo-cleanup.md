# Depuración del Repositorio OtelMS API — Plan de Limpieza

> **Para Hermes:** Usar subagent-driven-development para implementar este plan tarea por tarea.

**Goal:** Dejar el repo limpio y profesional: `.gitignore` completo, sin código viejo (pyotels), sin código muerto ni clases huérfanas, con baseline de tests verificado antes y después.

**Architecture:** Auditoría read-only → edición de `.gitignore` → eliminación de código legacy (pyotels) → eliminación de módulos huérfanos dentro de `otelms` → verificación completa (pytest + ruff + mypy + git status).

**Tech Stack:** Python 3.12, uv, pytest, ruff, mypy, git.

---

## Contexto actual (hallazgos del audit — 2026-08-01)

### Estado git
- **45 archivos trackeados**, de los cuales **32 son `src/pyotels/`** (el paquete VIEJO, reemplazado por `src/otelms/`)
- **`src/otelms/` completo está UNTRACKED** (78 archivos nuevos, la app real)
- Otros untracked: `docker/`, `migrations/`, `scripts/`, `.github/`, `.dockerignore`, `ARCHITECTURE.md`, `alembic.ini`, `tests/unit/`, `tests/integration/`, `tests/conftest.py`, `.hermes/`
- 4 archivos modificados sin commitear: `README.md`, `pyproject.toml`, `src/pyotels/scraper.py`, `tests/test_scraper.py`
- `pyproject.toml` ya fue renombrado de `pyotels` → `otelms-api` (solo falta limpiar el árbol)

### Código legacy a eliminar (fase 2)
| Ítem | Por qué es basura | Tamaño |
|------|-------------------|--------|
| `src/pyotels/` (32 archivos) | Paquete viejo reemplazado por `otelms`; nadie lo importa salvo `main.py`, `tests/test_scraper.py` y `docs/` (todos legacy también) | 32 archivos |
| `main.py` (raíz) | Script de prueba manual **con password hardcodeada** (`Majestic2`), importa `src.pyotels.*` | 1 archivo |
| `tests/test_scraper.py` | Test viejo de pyotels (unittest, importa `src.pyotels`); la suite real está en `tests/unit/` | 1 archivo |
| `docs/` (5 archivos) | Documentación vieja de pyotels (api_reference, configuration, index, installation, usage) — toda referenciando `pyotels`; la doc real es `README.md` + `ARCHITECTURE.md` | 5 archivos |
| `src/__init__.py` | Vacío (`"""  """`), innecesario con src layout | 1 archivo |
| `src/pyotels.egg-info/`, `src/otelms_api.egg-info/` | Artefactos de build no ignorados | 2 dirs |
| `otelms.db`, `test.db` | Bases de datos de dev generadas (ya ignoradas por `*.db`, solo borrar del disco) | 2 archivos |
| `http_cache/`, `test_cache/`, `logs/` | Runtime dirs sin ignorar (cache.db, otelms.log) | 3 dirs |

### Código muerto / huérfano dentro de `otelms` (fase 3)
| Módulo | Evidencia | Decisión propuesta |
|--------|-----------|--------------------|
| `src/otelms/domain/models/__init__.py` (19 clases Pydantic) | **0 imports** en todo el repo; duplica `api/schemas.py` (22 clases) | **Eliminar** (es duplicado muerto) |
| `src/otelms/scraping/wait_strategies.py` | Sin imports en src/ (solo su test `test_wait_strategies.py`) | **Eliminar** (decisión usuario: dead code) |
| `src/otelms/scraping/cache_layer.py` | Sin imports en src/ (solo su test `test_cache_layer.py`) | **Eliminar** (decisión usuario: dead code) |

### Baseline de tests (antes de tocar nada)
- Suite unit: **36 passed + 2 skipped** (requieren Redis) + **8 fallos pre-existentes** (parsers y repositories con SQLite — documentados, NO regresiones)
- Tests de features nuevas (websocket, wait_strategies, cache_layer, auth_2fa, adaptive_rate_limiter): **27 passed + 2 skipped** — baseline ANTES de eliminar wait_strategies/cache_layer y sus tests
- CI/CD existente: ruff check, ruff format --check, mypy src/otelms, pytest unit + integration

---

## Enfoque propuesto

1. **Fase 0**: Verificar baseline (tests + git status) y snapshot del estado.
2. **Fase 1**: Completar `.gitignore` (añadir caches, runtime dirs, egg-info) y **quitar `uv.lock`** del ignore (debe trackearse para reproducibilidad con uv).
3. **Fase 2**: Eliminar todo el árbol legacy (`pyotels`, `main.py`, `tests/test_scraper.py`, `docs/`, `src/__init__.py`).
4. **Fase 3**: Eliminar código muerto verificado dentro de `otelms` (`domain/models/`), confirmar con ruff F401 + pyflakes.
5. **Fase 4**: Verificación final — pytest, ruff, mypy, `git status` limpio y commit único de limpieza.

---

## Tareas

### Task 1: Baseline de tests y estado git

**Objective:** Registrar el estado actual antes de cualquier cambio.

**Files:**
- Ninguno (solo lectura)

**Step 1: Snapshot git**

Run: `git status --short && git ls-files | wc -l`
Expected: 45 archivos trackeados, 4 modified, 12 untracked

**Step 2: Baseline tests unit**

Run: `env -u PYTHONPATH .venv/Scripts/python.exe -m pytest tests/unit -q`
Expected: ~36 passed, 2 skipped, 8 failed (pre-existentes, documentar en el commit)

**Step 3: Registrar baseline**

Guardar el output en un archivo temporal (se borra al final) o en el mensaje del commit.

### Task 2: Completar `.gitignore`

**Objective:** Ignorar todo lo generado/runtime; trackear `uv.lock` y `.env.example`.

**Files:**
- Modify: `.gitignore`

**Step 1: Modificar `.gitignore`**

```gitignore
# --- Caches de herramientas ---
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Runtime del proyecto ---
http_cache/
test_cache/
logs/
*.egg-info/

# --- uv: el lock DEBE trackearse (reproducibilidad) ---
# (ELIMINAR la línea "uv.lock" del bloque de gestores de paquetes)

# --- .env.example sí se trackea ---
!.env.example
```

**Step 2: Verificar**

Run: `git check-ignore -v http_cache/cache.db logs/otelms.log .pytest_cache/ 2>&1`
Expected: cada uno reporta el patrón que lo ignora

**Step 3: Commit**

```bash
git add .gitignore
git -c user.name="Walter Cun" -c user.email="walte@local" commit -m "chore: completar .gitignore y trackear uv.lock"
```

### Task 3: Eliminar árbol legacy `pyotels`

**Objective:** Remover el paquete viejo y todo lo que solo existía para él.

**Files:**
- Delete: `src/pyotels/` (32 archivos), `main.py`, `tests/test_scraper.py`, `docs/`, `src/__init__.py`

**Step 1: Verificar que NADIE importa pyotels fuera de lo legacy**

Run: `grep -rn "pyotels" src/otelms/ tests/unit/ tests/integration/ docker/ .github/ 2>/dev/null | grep -v __pycache__`
Expected: **0 resultados** (solo quedan referencias en docs/ y main.py que también se borran)

**Step 2: Eliminar archivos**

```bash
git rm -r src/pyotels
git rm main.py tests/test_scraper.py src/__init__.py
git rm -r docs
rm -rf src/pyotels.egg-info src/otelms_api.egg-info http_cache test_cache logs otelms.db test.db
```

**Step 3: Verificar que el proyecto importa bien**

Run: `env -u PYTHONPATH .venv/Scripts/python.exe -c "from otelms.cli import app; from otelms.api.main import create_app; print('imports OK')"`
Expected: `imports OK`

**Step 4: Verificar que el lock de uv sigue consistente**

Run: `env -u PYTHONPATH uv lock --check`
Expected: exit 0 (lock sincronizado)

**Step 5: Commit**

```bash
git add -A
git -c user.name="Walter Cun" -c user.email="walte@local" commit -m "refactor: eliminar paquete legacy pyotels, scripts y docs obsoletos"
```

### Task 4: Eliminar código muerto en `otelms` — `domain/models/`, `wait_strategies`, `cache_layer`

**Objective:** Borrar los módulos huérfanos (duplicado de schemas + 2 módulos sin imports).

**Files:**
- Delete: `src/otelms/domain/models/` (`__init__.py`)
- Delete: `src/otelms/scraping/wait_strategies.py`
- Delete: `src/otelms/scraping/cache_layer.py`
- Delete (tests asociados): `tests/unit/test_wait_strategies.py`, `tests/unit/test_cache_layer.py`

**Step 1: Confirmar que NADIE los importa (doble check)**

Run: `grep -rn "domain.models\|domain import models\|from otelms.domain.models" src/otelms/ tests/ 2>/dev/null | grep -v __pycache__`
Expected: 0 resultados

Run: `grep -rn "wait_strategies\|cache_layer" src/otelms/ 2>/dev/null | grep -v __pycache__`
Expected: 0 resultados (solo los propios archivos)

**Step 2: Confirmar que las clases duplicadas viven en api/schemas.py**

Run: `grep -c "^class " src/otelms/api/schemas.py`
Expected: ≥ 19 (los schemas reales existen y son los que usan las rutas)

**Step 3: Eliminar**

```bash
rm -rf src/otelms/domain/models
rm src/otelms/scraping/wait_strategies.py src/otelms/scraping/cache_layer.py
rm tests/unit/test_wait_strategies.py tests/unit/test_cache_layer.py
```

**Step 4: Verificar imports de la app**

Run: `env -u PYTHONPATH .venv/Scripts/python.exe -c "from otelms.api.main import create_app; from otelms.domain.entities import Base; print('imports OK')"`
Expected: `imports OK`

**Step 5: Commit**

```bash
git add -A
git -c user.name="Walter Cun" -c user.email="walte@local" commit -m "refactor: eliminar domain/models huerfano (duplicado de api/schemas)"
```

### Task 5: Barrido final de código muerto con linters

**Objective:** Detectar cualquier import sin uso o símbolo huérfano restante.

**Files:**
- Solo lectura (análisis)

**Step 1: Ruff — imports sin usar (F401) y variables sin usar (F841)**

Run: `env -u PYTHONPATH .venv/Scripts/python.exe -m ruff check src/otelms --select F401,F841,F811,F822`
Expected: 0 errores (o corregir los que salgan, archivo por archivo)

**Step 2: Pyflakes (fallback si ruff no cubre algo)**

Run: `env -u PYTHONPATH .venv/Scripts/python.exe -m pyflakes src/otelms/ 2>&1 | head -20`
Expected: sin output

**Step 3: Buscar módulos `.py` sin importar (huérfanos)**

Run: `for f in $(find src/otelms -name "*.py" -not -name "__init__.py"); do base=$(basename $f .py); n=$(grep -rl "$base" src/otelms/ tests/ --include="*.py" 2>/dev/null | grep -v __pycache__ | grep -v "$f" | wc -l); if [ "$n" -eq 0 ]; then echo "POSIBLE HUERFANO: $f"; fi; done`
Expected: listar solo los módulos realmente sin referencia (ej. posibles restos)

**Step 4: Evaluar los huérfanos restantes detectados**

Los módulos ya decididos (wait_strategies/cache_layer) fueron eliminados en Task 4. Si el detector encuentra OTROS módulos sin referencia, revisarlos uno a uno y decidir con el usuario antes de eliminarlos.

**Step 5: Commit (si hubo correcciones)**

```bash
git add -A
git -c user.name="Walter Cun" -c user.email="walte@local" commit -m "refactor: limpiar imports sin uso detectados por ruff"
```

### Task 6: Verificación final integral

**Objective:** Todo verde antes de cerrar.

**Files:**
- Ninguno

**Step 1: Tests unit completos**

Run: `env -u PYTHONPATH .venv/Scripts/python.exe -m pytest tests/unit -q`
Expected: mismo resultado que baseline (36 passed + 2 skipped + 8 failed pre-existentes — **sin nuevos fallos**)

**Step 2: Ruff completo (como CI)**

Run: `env -u PYTHONPATH .venv/Scripts/python.exe -m ruff check . && env -u PYTHONPATH .venv/Scripts/python.exe -m ruff format --check .`
Expected: exit 0

**Step 3: MyPy (como CI)**

Run: `env -u PYTHONPATH .venv/Scripts/python.exe -m mypy src/otelms`
Expected: exit 0 (o solo errores pre-existentes documentados)

**Step 4: Tests de features nuevas (regresión rápida)**

Run: `env -u PYTHONPATH .venv/Scripts/python.exe -m pytest tests/unit/test_websocket.py tests/unit/test_auth_2fa.py tests/unit/test_adaptive_rate_limiter.py -q`
Expected: 13 passed + 2 skipped (wait_strategies/cache_layer eliminados con sus tests)

**Step 5: Estado git final**

Run: `git status --short`
Expected: solo los cambios de limpieza (sin archivos basura apareciendo)

**Step 6: Commit final si quedó algo**

```bash
git add -A
git -c user.name="Walter Cun" -c user.email="walte@local" commit -m "chore: limpieza final del repositorio"
```

---

## Archivos que cambiarán

| Acción | Ruta |
|--------|------|
| Modify | `.gitignore` |
| Delete | `src/pyotels/` (32 archivos) |
| Delete | `main.py`, `tests/test_scraper.py`, `src/__init__.py` |
| Delete | `docs/` (5 archivos) |
| Delete | `src/otelms/domain/models/` |
| Delete | `src/otelms/scraping/wait_strategies.py`, `src/otelms/scraping/cache_layer.py` |
| Delete | `tests/unit/test_wait_strategies.py`, `tests/unit/test_cache_layer.py` |
| Delete (disco, no git) | `src/pyotels.egg-info/`, `src/otelms_api.egg-info/`, `http_cache/`, `test_cache/`, `logs/`, `otelms.db`, `test.db` |

## Tests / validación

- `pytest tests/unit -q` → baseline intacto (sin regresiones nuevas)
- `ruff check .` + `ruff format --check .` → exit 0
- `mypy src/otelms` → sin errores nuevos
- `uv lock --check` → exit 0
- Smoke import: `python -c "from otelms.cli import app; from otelms.api.main import create_app"`

## Riesgos, tradeoffs y preguntas abiertas

1. **`wait_strategies.py` y `cache_layer.py`** — **DECISIÓN TOMADA (usuario): eliminar ambos** junto con sus tests. Se documenta en el commit para trazabilidad. Nota: al eliminar sus tests, el conteo de tests de features nuevas baja de 27 a 15 passed (websocket + auth_2fa + adaptive_rate_limiter).

2. **`uv.lock` trackeado** — Cambio intencional: con uv, el lock debe estar en git para `uv sync` reproducible (el CI ya lo usa). El `.gitignore` actual lo ignoraba — incoherencia con el CI/CD.

3. **Los 8 fallos pre-existentes** (parsers + repositories SQLite) NO se tocan en este plan — son deuda técnica de otra fase. Se documentan en el commit para no confundirlos con regresiones.

4. **`main.py` contenía credenciales hardcodeadas** (`Majestic2`) — se elimina; cualquier credencial real debe ir a `.env` (ya ignorado).

5. **`.agent/rules/rules.md`** — regla del proyecto que menciona `ia_tools/` y `temp_ia/` (dirs que ya no existen). No se toca (es directiva del usuario), solo se anota.

6. **¿Borrar también `tests/e2e/` y `tests/fixtures/` vacíos?** — Recomendado: sí, eliminar dirs vacíos en Task 3 (`rmdir`). No afectan git.
